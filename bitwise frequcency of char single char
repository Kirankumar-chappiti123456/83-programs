#include <stdio.h>
int main()
{
    char a[100],b;
    int c=0;
    printf("enter the stringand character:");
    scanf("%s %c",a,&b);
    int i=0;
    while(a[i]!='\0')
    {
        if((a[i]^b)==0)
        {
        c++;
        }
        i++;
    }
    printf("%c=%d",b,c);
}
